{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": null,
  "installCommand": "git clone --depth 1 --branch 3.44.8 https://github.com/flutter/flutter.git flutter && ./flutter/bin/flutter --version && ./flutter/bin/flutter config --enable-web && ./flutter/bin/flutter pub get",
  "buildCommand": "./flutter/bin/flutter build web --release",
  "outputDirectory": "build/web",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}